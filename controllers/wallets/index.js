const { getAllTotal } = require("./getAll");
const { createNew } = require("./createNew");
const { deleteW } = require("./deleteW");
const { renameW } = require("./renameW");
const { updateTotal } = require("./updateTotal");

module.exports = {
  getAllTotal,
  createNew,
  deleteW,
  renameW,
  updateTotal,
};
